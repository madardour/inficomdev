using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Xunit;

namespace UnitTestInficom
{
    [TestClass]
    public class UnitTest1
    {
        [Fact]
        public void TestMethod1()
        {
            //Mock<ILdapAuthentication> ldap = new Mock<ILdapAuthentication>();
            //ldap.Setup(m => m.IsAuthenticated(It.IsAny<bool>())).Returns(true);
            //ldap.Setup(m => m.GetAuthorisationGroups(It.IsAny<string>())).Returns(new List<string>());
        }
    }
}
